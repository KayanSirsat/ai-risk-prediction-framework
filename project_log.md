# Project Log — AI-Driven Risk Prediction Framework

This file tracks the development history, architectural decisions, and milestones.

---

## [2026-04-25] — Audit & Bug-Fix Session

**Phase:** Phase 2 Post-Completion Audit  
**Focus:** Identifying and resolving bugs from cross-referencing all source files against the PRD documents.

### Bugs Fixed (14 Total)

**🔴 Critical**
- **BUG-1** — GenAI model mismatch (dashboard used Llama, PRD mandates Qwen 3.5). Fixed: `genai_auditor.py` now delegates to `src/mitigation/llm_agent.py` — single source of truth.
- **BUG-2** — Phase 2 PRD was corrupted Python code. Fixed: Full 10-section PRD written covering F2-A through F2-F.
- **BUG-3** — `feature_columns.pkl` never generated by training pipeline. Fixed: Added `joblib.dump` in `train.py`; model retrained.
- **BUG-4** — `generate_paper_plots.py` crashed with `NameError`. Fixed: Both pipeline and raw-classifier branches now define `X_test_transformed`; `MODEL_DIR` constant added.
- **BUG-9** — Anomaly detector logging crashed on fresh clone (no `logs/` dir). Fixed: Added `os.makedirs("logs", exist_ok=True)` in `anomaly_detector.py`.

**🟠 High**
- **BUG-5** — PRD `<think>` tag mismatch vs code's `<reasoning>/<strategy>`. Fixed: PRD updated to document actual XML tag structure.
- **BUG-6** — Forecasting and Anomaly pages were empty stubs. Fixed: Full `render_forecasting_page()` and `render_anomaly_page()` implementations added to `dashboard.py`.
- **BUG-7** — Settings "Save" button did nothing. Fixed: Persistent JSON store at `logs/user_settings.json`; loaded on startup.
- **BUG-8** — Signup form created users that could never log in. Fixed: Persistent user registration store at `logs/demo_users.json`.

**🟡 Medium**
- **BUG-10** — Phase 1 achievements table had "TBD" in achieved column. Fixed: PRD updated.
- **BUG-11** — `forecast.py` `_fit_model()` ignored all config params. Fixed: Pre-initialized model now passed through correctly.
- **BUG-12** — Duplicate `_validate_data()` call in `generate_forecast()`. Fixed: Removed duplicate call.

**🔵 Low**
- **BUG-13** — `preprocessing/__init__.py` imported non-existent `DataPipeline`. Fixed: Updated to export actual public functions.
- **BUG-14** — Dead `src/phase2/` placeholder directory. Fixed: Deleted.

### Achievements
- All 44 existing tests continued passing after fixes
- Added 32 new tests (What-If simulator, Jira OAuth, Phase 2 forecasting/anomaly integration)
- Total: 76/76 tests passing

---

## [2026-04-13] — Phase 2 Commit Push

**Phase:** Phase 2 Implementation Complete  
**Focus:** Committing all Phase 2 work in 10 logical commits.

### Accomplishments
- Pushed `feat(forecasting)`: Prophet forecasting with 14-day sprint seasonality
- Pushed `feat(anomaly)`: Isolation Forest with severity tiers and ROC evaluation
- Pushed `feat(dashboard)`: Forecasting and anomaly live analytics in dashboard
- Pushed `feat(integrations)`: Jira REST client and OAuth handler
- Pushed `feat(views)`: What-If simulator, Jira Sync, Ticket Auditor, Settings pages
- Pushed `test(phase2)`: All Phase 2 integration and unit tests

---

## [2026-04-12] — Phase 2 Audit & Cleanup

**Phase:** Phase 2 Stabilisation  
**Focus:** Cleaning up the codebase, fixing import errors, establishing consistent design.

### Accomplishments
- Removed all deprecated fallback logic and redundant code
- Fixed persistent import errors with proper `pyproject.toml` `editable` install
- Unified Streamlit styling using centralised `app/utils/styles.py`
- Established `app/views/` architecture with lazy `__getattr__` imports
- Created `app/utils/routes.py` for safe page navigation

---

## [2026-04-09] — Domain-First Architecture Restructure

**Phase:** Phase 2 → Production  
**Focus:** Moving from models-centric to domain-first folder layout.

### Accomplishments
- Moved `nlp_risk_engine.py` → `src/nlp/` and `anomaly_detector.py` → `src/anomaly/`
- Deleted `src/models/` folder entirely
- Updated all 5 test files and 3 `__init__.py` files
- All 44 tests passed after restructuring

---

## [2026-04-07] — Phase 1 Dashboard & GenAI Integration

**Phase:** Phase 1 → Phase 2 Transition  
**Focus:** UI dashboard, XAI, GenAI integration.

### Accomplishments
- Built complete Streamlit dashboard (`app/main.py`) with "Jira-Minimalist" design
- Generated IEEE paper artefacts: ROC-AUC curve and Confusion Matrix
- Integrated SHAP explainability: normalized SHAP values as percentage influence bars
- Built `src/mitigation/llm_agent.py` (Qwen 3.5 via NVIDIA API with exponential backoff)
- Implemented `st.session_state` TTL caching for AI recommendations

---

## [2026-04-05] — Stage 1 → Stage 2 Transition

**Phase:** Stage 1 → Stage 2  
**Focus:** Repository clean-up and baseline implementation.

### Accomplishments
- Consolidated roadmap into README; initialized project_log.md
- Created `src/preprocessing/data_pipeline.py` — synthetic dataset with ML-ready features
- Created `src/training/train.py` — RandomForest + XGBoost training with leakage prevention
- Verified architecture placeholder files against `module_overview.md`
